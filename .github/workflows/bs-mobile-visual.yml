#name: VISUAL MOBILE tests via BrowserStack
#
#on:
##  pull_request:
##    branches:
##      - main
#
#  schedule:
#    - cron: '0 22 * * *'
#  workflow_dispatch:
#
#jobs:
#  run-tests:
#    name: 'Run tests on BrowserStack'
#    runs-on: ubuntu-latest
#    env:
#      BROWSERSTACK_CONFIG_FILE: browserstack-visual-mobile.yml
#
#    steps:
#      - name: Git clone
#        uses: actions/checkout@v4
#
#      - name: Write browserstack-visual-mobile.yml from secret
#        env:
#          CONFIG_BS: ${{ secrets.BROWSERSTACK_VISUAL_MOBILE }}
#        run: |
#          echo "$CONFIG_BS" > browserstack-visual-mobile.yml
#
#      - name: Set up Java
#        uses: actions/setup-java@v4
#        with:
#          distribution: 'corretto'
#          java-version: '17'
#
#      - name: Install dependencies
#        run: mvn -B -q -DskipTests package
#
#      - name: Run Desktop suite on BrowserStack
#        run: mvn -B test -Pmobile-visual-test